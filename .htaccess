# Redirect API requests to the PHP API
RewriteEngine On
RewriteRule ^api/(.*)$ apps/php-api/public/index.php [QSA,L]

# Serve static files directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule .* - [L]

# Serve UI for other requests
RewriteRule ^(.*)$ apps/ui/public/index.html [QSA,L]
